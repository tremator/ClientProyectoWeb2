import { Injectable } from '@angular/core';
import { SEARCH, NEWS_FILTER, CHARGE, USER_TAGS } from './queries';

@Injectable({
  providedIn: 'root'
})
export class GraphQLServiceService {

  constructor() { 
    
  }
}
